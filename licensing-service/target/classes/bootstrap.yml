spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
#      uri: http://localhost:8888      
#      discovery:
#        enabled: true
#        serviceId: configserver
